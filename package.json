{
	"name": "large-file-sorting",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"prebuild": "rimraf dist",
		"typecheck": "tsc --project tsconfig.json",
		"file-sort": "node --expose-gc dist/main.js",
		"file-sort:build": "rspack build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"large",
		"file",
		"sorting"
	],
	"author": "Nevsky Vladislav",
	"license": "MIT",
	"devDependencies": {
		"@rspack/cli": "^0.7.5",
		"@rspack/core": "^0.7.5",
		"@types/node": "^20.14.10",
		"rimraf": "^6.0.1",
		"tsc-alias": "^1.8.10"
	},
	"dependencies": {
		"dotenv": "^16.4.5"
	}
}
