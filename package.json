{
	"name": "large-file-sorting",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"prebuild": "rimraf dist",
		"typecheck": "tsc --project tsconfig.json",
		"file-sort": "node dist/main.js",
		"file-sort:build": "npm run prebuild && tsc",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"large",
		"file",
		"sorting"
	],
	"author": "Nevsky Vladislav",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.14.10",
		"rimraf": "^6.0.1"
	},
	"dependencies": {
		"dotenv": "^16.4.5"
	}
}
